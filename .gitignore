node_modules
.yarn
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
webroot
dist
coverage

# Editor / IDE state — user-specific, not project config.
.idea/
.vscode/

# Claude Code: launch.json is shared project config and IS committed;
# settings.local.json is per-machine and is not.
.claude/settings.local.json

# Obsidian per-machine editor state (the vault content is tracked, its UI config is not)
game_design/.obsidian/
